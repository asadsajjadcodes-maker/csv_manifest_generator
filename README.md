# csv_manifest_generator
 A high-performance Python desktop application built with PySide6 for file system auditing and automated asset manifest generation. The tool deeply scans nested directory structures, extracts critical OS file metadata (file size, extension, parent directory, creation/modification timestamps), and exports structured CSV reports. Features dynamic filtering options to audit specific file types and lightweight path resolution using Python's native pathlib and os.stat modules.
